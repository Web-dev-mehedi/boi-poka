import React from 'react';
import PropTypes from 'prop-types';

const PageToRead = props => {
    return (
        <div>
            <h1>hello form PageToRead</h1>
        </div>
    );
};

PageToRead.propTypes = {
    
};

export default PageToRead;